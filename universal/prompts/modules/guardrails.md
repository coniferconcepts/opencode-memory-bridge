# Universal Guardrails\n\n## Critical Rules (NEVER Violate)\n- NEVER use TanStack Query - Legend State v3 ONLY for state management\n- NEVER use Zod/Yup - Valibot ONLY for validation\n- NEVER use Tailwind - Tamagui tokens ONLY for styling\n- NEVER use npm/pnpm - Bun ONLY for package management\n- NEVER bypass fieldId mapping in Legend State - ALWAYS use transformers\n- NEVER commit secrets or API keys to code\n- NEVER skip input validation on user-facing endpoints\n\n## Technology Stack\n- **State**: Legend State v3 with syncedCrud\n- **Validation**: Valibot with type inference\n- **UI**: Tamagui with design tokens\n- **API**: tRPC + Hono on Cloudflare Workers\n- **Database**: Drizzle ORM + Cloudflare D1\n- **Runtime**: Bun\n- **Linting**: Biome (not ESLint/Prettier)\n- **Auth**: Better Auth\n- **Testing**: Vitest\n\n## Field Mapping (P04 Critical)\n- ALWAYS use fieldId to match API/database field names\n- NEVER direct field mapping - ALWAYS use createEntityTransformer\n- ALWAYS run `bun run validate:fields` before committing\n- Data loss risk if fieldId mapping is incorrect\n